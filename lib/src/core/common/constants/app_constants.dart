import 'package:flutter/material.dart';
import 'package:set_game/src/core/common/constants/sizes.dart';

class AppConst {
  static const cardAspectRatio = 3/2;
  static final cardBorderRadius = BorderRadius.circular(p16);

  static const elevateCardAnimatinoDuration = Duration(milliseconds: 500);
  static const correctSetAnimationDuration = Duration(milliseconds: 1700);
  static const incorrectSetAnimationDuration = Duration(milliseconds: 500);
}